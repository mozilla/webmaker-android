var React = require('react');
var render = require('../../lib/render.jsx');

var ApiTest = React.createClass({
  render: function () {
    return (<div>
      API tests
    </div>);
  }
});

render(ApiTest);
